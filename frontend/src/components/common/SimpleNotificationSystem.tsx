import React from 'react';

// A simplified notification system component for testing
const SimpleNotificationSystem: React.FC = () => {
  return (
    <div className="fixed top-4 right-4 z-50 w-full max-w-xs" data-testid="notification-system">
      {/* This is a simplified notification system for testing */}
    </div>
  );
};

export default SimpleNotificationSystem;
